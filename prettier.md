```js
const config = {
	arrowParens: "avoid", // 箭头函数只有一个参数不要括号
	bracketSameLine: true, // html 中 ">" 与上一行连着，不单独转行
	bracketSpacing: false, // 参数与左右括号之间去除空格
	endOfLine: "auto", // 结尾换行格式 lf
	experimentalTernaries: false, // 保留三元组的默认行为；将问号与结果保持在同一行
	jsxSingleQuote: false, // jsx 中使用单引号
	printWidth: 200, // 不换行的最大长度
	proseWrap: "preserve", // 什么也不做，保持散文原样
	semi: true, // 结尾添加分号
	singleQuote: false, // 使用双引号
	tabWidth: 2, // 缩进空格数
	useTabs: true, //使用 tab 键缩进
	trailingComma: "none" // 没有尾随逗号
};
```
